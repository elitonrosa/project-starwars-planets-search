import React from 'react';
import SearchInput from './SearchInput';
import Table from './Table';

function Main() {
  return (
    <main>
      <section>
        <SearchInput />
      </section>
      <section>
        <Table />
      </section>
    </main>
  );
}

export default Main;
